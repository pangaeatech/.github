name: NPM Audit

on:
  push:
    branches: [$default-branch]
  schedule:
    - cron: "0 0 * * *"

jobs:
  npm-audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2
        with:
          node-version: "14"

      - run: npm audit --production # Use yarn instead of npm if applicable
        working-directory: ./ # Set to your working directory
